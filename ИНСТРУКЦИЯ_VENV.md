# Инструкция по созданию виртуального окружения

## ✅ Быстрый способ

**Дважды кликните на файл:** `СОЗДАТЬ_VENV.bat`

Этот скрипт автоматически:
1. Проверит наличие Python
2. Удалит старое окружение (если есть)
3. Создаст новое виртуальное окружение
4. Покажет инструкции по активации

## 📋 Ручной способ

### Шаг 1: Откройте командную строку в папке проекта

```bash
cd "C:\Users\Евгений\Documents\unibot-main\unibot-main"
```

### Шаг 2: Создайте виртуальное окружение

**Если Python 3.11 установлен:**
```bash
py -3.11 -m venv .venv
```

**Или используйте текущий Python:**
```bash
python -m venv .venv
```

### Шаг 3: Активируйте окружение

```bash
.venv\Scripts\activate.bat
```

### Шаг 4: Обновите pip

```bash
python -m pip install --upgrade pip
```

### Шаг 5: Установите зависимости

```bash
pip install -r requirements.txt
pip install -r requirements-dev.txt
```

## 🔍 Проверка

После активации окружения проверьте:

```bash
python --version
```

Должно показать версию Python из виртуального окружения.

## 📝 Использование Python 3.11

Если вы хотите использовать именно Python 3.11:

1. **Убедитесь, что Python 3.11 установлен:**
   ```bash
   py -3.11 --version
   ```

2. **Создайте окружение с Python 3.11:**
   ```bash
   py -3.11 -m venv .venv
   ```

3. **Активируйте и установите зависимости:**
   ```bash
   .venv\Scripts\activate.bat
   pip install -r requirements.txt
   pip install -r requirements-dev.txt
   ```

## ⚠️ Важно

- Виртуальное окружение создаётся в папке `.venv` в корне проекта
- Активируйте окружение каждый раз перед работой с проектом
- После активации команда `python` будет использовать Python из окружения

## 🚀 После создания окружения

1. Активируйте окружение: `.venv\Scripts\activate.bat`
2. Установите зависимости: `pip install -r requirements.txt -r requirements-dev.txt`
3. Примените миграции: `alembic upgrade head`
4. Запустите бота: `uvicorn src.main:app --reload`




