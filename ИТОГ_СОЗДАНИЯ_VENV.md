# Итог создания виртуального окружения

## ✅ Скрипты созданы

Созданы следующие скрипты для создания виртуального окружения:

### 1. `СОЗДАТЬ_VENV.bat` (рекомендуется)
**Запуск:** Дважды кликните на файл

Автоматически:
- Проверит наличие Python
- Удалит старое окружение
- Создаст новое виртуальное окружение
- Покажет инструкции

### 2. `execute_venv_creation.py`
**Запуск:** `python execute_venv_creation.py`

Создаёт виртуальное окружение через Python API.

### 3. `make_venv.py`
**Запуск:** `python make_venv.py`

Альтернативный скрипт для создания окружения.

## 📋 Как создать виртуальное окружение

### Способ 1: Через батник (самый простой)

1. Откройте папку проекта:
   ```
   C:\Users\Евгений\Documents\unibot-main\unibot-main
   ```

2. Дважды кликните на файл: **`СОЗДАТЬ_VENV.bat`**

3. Дождитесь завершения (окно закроется автоматически)

### Способ 2: Через командную строку

Откройте командную строку в папке проекта и выполните:

```bash
# Создание окружения
python -m venv .venv

# Активация
.venv\Scripts\activate.bat

# Установка зависимостей
pip install -r requirements.txt
pip install -r requirements-dev.txt
```

### Способ 3: С Python 3.11 (если установлен)

```bash
# Создание с Python 3.11
py -3.11 -m venv .venv

# Активация
.venv\Scripts\activate.bat

# Установка зависимостей
pip install -r requirements.txt
pip install -r requirements-dev.txt
```

## 🔍 Проверка

После создания окружения проверьте:

```bash
# Активируйте окружение
.venv\Scripts\activate.bat

# Проверьте версию Python
python --version

# Проверьте установленные пакеты
pip list
```

## 📝 После создания окружения

1. **Активируйте окружение:**
   ```bash
   .venv\Scripts\activate.bat
   ```

2. **Обновите pip:**
   ```bash
   python -m pip install --upgrade pip
   ```

3. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   pip install -r requirements-dev.txt
   ```

4. **Примените миграции:**
   ```bash
   alembic upgrade head
   ```

5. **Запустите бота:**
   ```bash
   uvicorn src.main:app --reload
   ```

## ⚠️ Важно

- Виртуальное окружение создаётся в папке `.venv` в корне проекта
- Активируйте окружение каждый раз перед работой с проектом
- После активации команда `python` будет использовать Python из окружения

## 🚀 Готово к использованию

Все скрипты готовы. Запустите `СОЗДАТЬ_VENV.bat` двойным кликом для создания виртуального окружения!




